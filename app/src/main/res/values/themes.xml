<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FindMovie" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.FindMovie" parent="Base.Theme.FindMovie" />

    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name = "windowSplashScreenAnimatedIcon" >@drawable/icon_main</item>
        <item name="windowSplashScreenBackground">@color/auth_color</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.FindMovie</item>

    </style>
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorControlNormal">@color/auth_border</item>
        <item name="colorControlActivated">@color/auth_border</item>
    </style>

    </resources>